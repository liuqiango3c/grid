行政区划数据，一般为导入，全国统一编码，在实施过程中，根据情况，可能只需要取数据的自己，比如某个省的部分。
一般考虑静态化数据，可以根据地址联动控件要求提供数据比如
浙江的地市变量，保存地市编码与名称，
浙江的县区编码，保存市/县区数据的编码与名称。
浙江的县乡编码，保存县/乡数据，可以一个文件，也可以按目录分县保存。

考虑到省市县区的信息一般比较少且都会完整，而乡镇，街道，村组/社区信息可能不全的情况，地址信息保存分2个字段保存district, subdistrict, region:{ type:, id:,name}

地址编码信息
address:{
    district: 省市县区的编码                  //订单地址模式匹配确定district
    subdistrict：乡镇街道/村组社区 的编码      //订单地址模式匹配确定subdistrict,可选
    region:{                                 //订单地址进行点状区域匹配，带状匹配，片状匹配。
         type:nodal //表示地址区域为点状区域，其它可选的为belt(带区区域)，poly(多边形区域，GEO)，org(组织名称)：
         code:value //点状区域匹配的编码，获取小区，大楼名
         params:{}  //
     }
    subregion:{ //不需要
       
    }
    
}

