关键字段

workcardid:工单号
orderid:关联订单号 参考订单表 工单与订单之间的关系1:1, 1:N?
status:总状态，根据逻辑可以判断工单的总状态， 与udatetime配合可以直到每种状态的到大时间？
       new（新工单）, 
       dispatch(派单中),
       process（执行中）,
       done(完成),
       cancel(取消),
       suspend（挂起）。
       
dispatch：工单派单状态     
       idle(未派), 
       grabing（抢单模式派发）, 
       assigning（指定模式派发），
       assigned （已派单）, //派给指定人，已经接收
       grabed （已派单）,   //派给抢单，已被抢
       
feedback：工单派单反馈状态 
       idle(不需要)
       waiting（等待反馈）
       rejected（派发被拒绝）, 
       
process： 工单执行状态
       reserving(待预约)，
       reserved（已预约）,
       installed（已安装）

comments：工单订户评价数量
granularity：工单派发粒度
when：工单派发类型
mode: 工单派发模式
priority:工单优先级
startup：工单发起方
cdatetime:工单生成时间 
udatetime:最后更新时间
ddatetime:最后派发时间，每次派发后更新，保留派单的时间
dexpire:最后派发超时时间，每次派发后更新，为最后派发时间+超时时长。
ftime:最后反馈时间，反馈时间减去最后派发时间的差值（单位S），工单被接收或者拒绝时间。

单条记录只记录最后时间，历史操作记录通过revision获得。比如操作过程，状态迁移，派发历史等等。

重发流程
1 当dispatch状态为new时候，


